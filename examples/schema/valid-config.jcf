name = "my-awesome-app"
version = "1.2.3"
port = 8080
environment = "production"

features = ["auth", "api", "websockets"]

database = (
    host = "localhost",
    port = 5432,
    name = "myapp_db"
)
